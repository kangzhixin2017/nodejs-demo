/**
 * 1.推荐创建完目录就创建package.json
 *   npm init  或者 npm init --yes
 * 2.安装cnpm：
 *   npm install -g cnpm --registry=https://registry.npm.taobao.org
 * 3.package目录
 *   ^ 表示第一位版本号不变，后面两位取最新
 *   ~ 表示前两位不变，最后一个取最新
 *   * 表示全部取最新
 *   固定版本号不变就不用写
 */
